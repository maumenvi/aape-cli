/** Resolved filesystem paths used by the catalog store. */
export interface CatalogStorePaths {
  manifest: string;
  lock: string;
  contextDir: string;
  contextDev: string;
  contextLlm: string;
  vscodeMcp: string;
}
